# balanced_metrics_layout / 多指标均衡网格型

适合综合运营、数据总览、多部门看板等没有强主视觉，但有多个业务主题的场景。

## 结构原则

- 槽位只限制大致位置和优先级，不固定放什么内容。先分析指标，主指标或最高优先级主题组进视觉重心，其他指标按优先级分布。
- 不按固定九宫格生成。
- 先按业务主题分组，再决定每组使用 KPI、图表、列表或页签。
- 可设置一个相对强调的 `PrimarySlot`，但不强制中心主视觉。

## Shell 规则

普通业务模块默认 `CardShell`。只有无标题组合底板才使用 `PanelShell`。
